package fr.lucreeper74.createmetallurgy.content.items;

import net.minecraft.world.item.Item;

public class CokeItem extends Item {
    public CokeItem(Properties properties) {
        super(properties);
    }

    // Burn time will be set via data map or event handler
    // For now, this is a placeholder that can be extended later
}
